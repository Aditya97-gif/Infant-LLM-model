# Infant-LLM-model
A basic LLM model created using python and it's useful library such as numpy, pytorch and trained on the verdict text with the parameters taken from chatgpt 3 model. The model is trained to answer all questions related to the trained dataset. This is the first version of the model hence needs more refinement to procure the best results.
